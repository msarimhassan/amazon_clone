import React from 'react';

import CardHandler from './CardHandler';

const HomePage = () => {
    return (
        <div>
            <CardHandler />
        </div>
    );
};

export default HomePage;
